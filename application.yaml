spring:
   datasource:
     username: song_catalog
     password: song_catalog
     url: jdbc:mysql://localhost:3306/song_catalog
      
   jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
     defer-datasource-initialization: true
     
   sql:
    init:
      mode: never  